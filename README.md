# dev-env

🛠️ A personal script to bootstrap my development environment on a fresh Linux system.

## Usage

```bash
git clone https://github.com/YOUR_USERNAME/dev-env.git ~/personal/dev-env
cd ~/personal/dev-env
DEV_ENV=$(pwd) ./dev-env

